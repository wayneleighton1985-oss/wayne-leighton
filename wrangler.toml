# Cloudflare Pages Configuration

name = "wayne-leighton"
compatibility_date = "2023-12-01"
pages_build_output_dir = "dist"
# Removed build configuration as it's not supported in Cloudflare Pages projects